<h1>Carteiras</h1>
<div class="list-group">
  <% @carteiras.each do |carteira| %>
    <%= link_to carteira.nome, carteira_path(carteira), class: "list-group-item list-group-item-action" %>
  <% end %>
</div>
