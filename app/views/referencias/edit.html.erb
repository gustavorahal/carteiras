<%= render 'common/titulo_pagina', titulo: "Editar ativo para carteira #{@carteira.nome}" %>

<%= bootstrap_form_for [@carteira, @carteira_ativo],
                       url: carteira_carteira_referencia_path(@carteira, @carteira_ativo) do |f| %>

  <%= render 'form', f: f %>

<% end %>